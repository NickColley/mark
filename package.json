{
  "name": "mark",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify -t reactify ./index.js -o ./app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "marked": "^0.3.3",
    "react": "^0.13.3"
  },
  "devDependencies": {
    "browserify": "^10.2.0",
    "reactify": "^1.1.1"
  }
}
